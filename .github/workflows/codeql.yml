name: CodeQL setup guard

on:
  workflow_dispatch:

permissions: {}

jobs:
  default-setup:
    name: Default setup owns CodeQL
    runs-on: ubuntu-latest
    steps:
      - name: Explain configuration
        run: |
          echo "CodeQL is managed by GitHub code scanning Default setup."
          echo "This workflow intentionally does not upload SARIF because advanced uploads are rejected while Default setup is enabled."
